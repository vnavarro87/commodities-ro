name: Keep Alive

on:
  schedule:
    - cron: '0 12 * * 1'  # toda segunda-feira ao meio-dia UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://graos-ro.streamlit.app/ > /dev/null
